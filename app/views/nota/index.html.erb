<p id="notice"><%= notice %></p>

<h1>Nota</h1>

<table>
  <thead>
    <tr>
      <th>Notaaluno</th>
      <th>Notatrabalhos</th>
      <th>Notalivro</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nota.each do |notum| %>
      <tr>
        <td><%= notum.notaaluno %></td>
        <td><%= notum.notatrabalhos %></td>
        <td><%= notum.notalivro %></td>
        <td><%= link_to 'Show', notum %></td>
        <td><%= link_to 'Edit', edit_notum_path(notum) %></td>
        <td><%= link_to 'Destroy', notum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Notum', new_notum_path %>
